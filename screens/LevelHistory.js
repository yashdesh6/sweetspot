import { View, Text, ScrollView, TouchableOpacity } from 'react-native';
const LevelHistory = ({navigation}) => {

    return (
        <Text className='text-orange-500'>hello world</Text>
    );
  };
  
  export default LevelHistory;